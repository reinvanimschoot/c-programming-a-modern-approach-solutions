#include <stdio.h>

int main(void)
{
  int foo, bar, baz;
  float a, b, c;

  printf("Integers - foo: %d, bar: %d, baz: %d\n", foo, bar, baz);
  printf("Floats - a: %f, b: %f, c: %f\n", a, b, c);

  return 0;
}
